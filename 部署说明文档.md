# Apache ShenYu 打包部署说明文档

## 目录
- [项目概述](#项目概述)
- [环境要求](#环境要求)
- [打包步骤](#打包步骤)
- [Docker 镜像构建](#docker-镜像构建)
- [生成部署包](#生成部署包)
- [服务器部署](#服务器部署)
- [配置说明](#配置说明)
- [服务启动与验证](#服务启动与验证)
- [监控部署（可选）](#监控部署可选)
- [故障排除](#故障排除)

---

## 项目概述

Apache ShenYu 是一个异步的、高性能的、跨语言的、响应式的 API 网关。

- **项目版本**: 2.7.0.2-SNAPSHOT
- **Java 版本**: Java 17
- **构建工具**: Maven
- **容器化**: Docker
- **主要组件**:
  - `shenyu-admin`: 管理后台（端口 9095）
  - `shenyu-bootstrap`: API 网关（端口 9195）

---

## 环境要求

### 开发/构建环境
- **操作系统**: Windows / Linux / macOS
- **Java**: JDK 17 或更高版本
- **Maven**: 3.6+ （项目已包含 Maven Wrapper `mvnw`）
- **Docker**: 20.10+ （支持多架构构建）
- **磁盘空间**: 至少 5GB 可用空间

### 生产环境
- **操作系统**: Linux（推荐 CentOS 7+ / Ubuntu 18.04+）
- **Docker**: 20.10+ 
- **MySQL**: 5.7+ / 8.0+ （用于存储配置数据）
- **内存**: 至少 2GB RAM
- **磁盘空间**: 至少 10GB 可用空间

---

## 打包步骤

### 1. 克隆项目（如果尚未克隆）

```bash
git clone <repository-url>
cd shenyu
```

### 2. 使用 Maven 打包项目

本地打包

```
mvn clean package -Prelease -Drat.skip=true -Dcheckstyle.skip=true -Dmaven.javadoc.skip=true -Dmaven.test.skip=true
```

注意：注意需要把mysql和依赖包含上， 以及需要勾选 release 的profile

- 构建产物位于 `shenyu-dist/shenyu-admin-dist/target/` 和 `shenyu-dist/shenyu-bootstrap-dist/target/`

### 3. 验证打包结果

构建完成后，检查以下目录：

```bash
# Admin 打包结果
ls -lh shenyu-dist/shenyu-admin-dist/target/apache-shenyu-2.7.0.2-SNAPSHOT-admin-bin.tar.gz

# Bootstrap 打包结果
ls -lh shenyu-dist/shenyu-bootstrap-dist/target/apache-shenyu-2.7.0.2-SNAPSHOT-bootstrap-bin.tar.gz
```

---

## Docker 镜像构建

项目支持构建 x86_64 (amd64) 和 ARM64 (aarch64) 两种架构的 Docker 镜像。

### 构建 x86_64 架构镜像

```bash
# 执行构建脚本
./build-x86.sh
```

脚本会自动：
1. 清除旧镜像
2. 拉取 x86_64 架构的基础镜像（alpine、amazoncorretto:17）
3. 构建 `shenyu-admin:latest-x86` 镜像
4. 构建 `shenyu-bootstrap:latest-x86` 镜像
5. 验证镜像架构

**注意**: 脚本会使用 `--platform linux/amd64` 确保构建的是 x86_64 架构镜像。

### 构建 ARM64 架构镜像

```bash
# 执行构建脚本
./build-arm.sh
```

脚本会自动：
1. 清除旧镜像
2. 拉取 ARM64 架构的基础镜像
3. 构建 `shenyu-admin:latest-arm` 镜像
4. 构建 `shenyu-bootstrap:latest-arm` 镜像
5. 验证镜像架构

### 验证镜像

```bash
# 查看镜像列表
docker images | grep shenyu

# 检查镜像架构
docker image inspect shenyu-admin:latest-x86 --format='{{.Architecture}}'
docker image inspect shenyu-bootstrap:latest-x86 --format='{{.Architecture}}'
```

---

## 生成部署包

### 1. 保存 Docker 镜像

在构建镜像后，需要将镜像保存为 tar 文件以便离线部署：

#### 保存 x86 架构镜像

```bash
./save-images.sh x86
```

输出目录: `offline-images/x86/`

包含文件:
- `alpine.tar` - 基础镜像
- `amazoncorretto-17-alpine.tar` - JDK 镜像
- `shenyu-admin-x86.tar` - Admin 应用镜像
- `shenyu-bootstrap-x86.tar` - Bootstrap 应用镜像
- `load-images.sh` - 镜像加载脚本

#### 保存 ARM 架构镜像

```bash
./save-images.sh arm
```

输出目录: `offline-images/arm/`

包含文件:
- `alpine.tar` - 基础镜像
- `amazoncorretto-17-alpine.tar` - JDK 镜像
- `shenyu-admin-arm.tar` - Admin 应用镜像
- `shenyu-bootstrap-arm.tar` - Bootstrap 应用镜像
- `load-images.sh` - 镜像加载脚本

### 2. 创建完整部署包

部署包包含所有必要的文件，可以直接传输到服务器进行部署。

#### 创建 x86 架构部署包

```bash
./create-deployment-package.sh x86
```

#### 创建 ARM 架构部署包

```bash
./create-deployment-package.sh arm
```

#### 创建包含所有架构的部署包

```bash
./create-deployment-package.sh all
```

**输出**: `shenyu-deployment-package-<架构>-<日期>.tar.gz`

**部署包内容**:
```
shenyu-deployment-package-<架构>-<日期>/
├── build-x86.sh                    # x86 构建脚本（本地使用）
├── build-arm.sh                    # ARM 构建脚本（本地使用）
├── save-images.sh                  # 镜像保存脚本（本地使用）
├── deploy-server.sh                # 服务器部署脚本（自动检测架构）
├── offline-images/                 # 离线 Docker 镜像
│   ├── x86/                        # x86_64 架构镜像（如果包含）
│   └── arm/                        # ARM64 架构镜像（如果包含）
├── docker-compose-monitoring.yml   # Prometheus & Grafana 编排文件
├── start-monitoring.sh             # 启动监控脚本
├── stop-monitoring.sh              # 停止监控脚本
├── monitoring/                     # 监控配置和面板
│   ├── prometheus.yml
│   └── grafana/
└── README.md                       # 部署包说明文档
```

---

## 服务器部署

### 1. 传输部署包到服务器

```bash
# 使用 SCP 传输
scp shenyu-deployment-package-<架构>-<日期>.tar.gz user@server:/tmp/

# 或使用其他方式（FTP、rsync 等）
```

### 2. 在服务器上解压

```bash
# SSH 登录服务器
ssh user@server

# 解压部署包
cd /tmp
tar -xzf shenyu-deployment-package-<架构>-<日期>.tar.gz
cd shenyu-deployment-package-<架构>-<日期>
```

### 3. 运行部署脚本

部署脚本会自动检测服务器架构并加载对应的镜像：

```bash
# 赋予执行权限（如果需要）
chmod +x deploy-server.sh

# 执行部署脚本
./deploy-server.sh
```

**部署脚本功能**:
1. 自动检测服务器架构（x86_64 或 aarch64）
2. 加载对应架构的 Docker 镜像
3. 创建部署目录 `/opt/shenyu`
4. 生成配置文件模板
5. 创建启动脚本

**部署目录结构**:
```
/opt/shenyu/
├── admin/                    # Admin 数据目录
├── bootstrap/                # Bootstrap 数据目录
├── configs/                  # 配置文件目录
│   ├── admin/
│   │   ├── application.yml
│   │   └── application-mysql.yml
│   └── bootstrap/
│       └── application.yml
├── logs/                     # 日志目录
│   ├── admin/
│   └── bootstrap/
├── start-admin.sh            # 启动 Admin 脚本
├── start-bootstrap.sh        # 启动 Bootstrap 脚本
├── stop-all.sh               # 停止所有服务脚本
└── restart-all.sh            # 重启所有服务脚本
```

---

## 配置说明

### 1. 配置 MySQL 数据库

编辑 `/opt/shenyu/configs/admin/application-mysql.yml`:

```yaml
spring:
  datasource:
    url: jdbc:mysql://<数据库地址>:3306/shenyu?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=convertToNull&allowPublicKeyRetrieval=true
    username: <数据库用户名>
    password: <数据库密码>
    driver-class-name: com.mysql.cj.jdbc.Driver
```

**重要**: 确保 MySQL 数据库已创建 `shenyu` 数据库，ShenYu 会在首次启动时自动初始化表结构。

### 2. 配置 Admin

编辑 `/opt/shenyu/configs/admin/application.yml`:

主要配置项：
- `server.port`: Admin 服务端口（默认 9095）
- `shenyu.sync.websocket.allowOrigins`: WebSocket 允许的源地址
- `shenyu.register.registerType`: 注册中心类型（http/websocket/zookeeper/nacos 等）

### 3. 配置 Bootstrap

编辑 `/opt/shenyu/configs/bootstrap/application.yml`:

主要配置项：
- `server.port`: Bootstrap 服务端口（默认 9195）
- `shenyu.sync.websocket.urls`: WebSocket 同步地址（指向 Admin）
- `shenyu.register.registerType`: 注册中心类型

### 4. 环境变量配置

如果需要通过环境变量配置，可以在启动脚本中添加：

```bash
# 编辑 start-admin.sh
docker run -d \
  --name shenyu-admin \
  --restart unless-stopped \
  -p 9095:9095 \
  -v /opt/shenyu/configs/admin:/opt/shenyu-admin/conf \
  -v /opt/shenyu/logs/admin:/opt/shenyu-admin/logs \
  -e SPRING_PROFILES_ACTIVE=mysql \
  -e MYSQL_HOST=192.168.1.100 \
  -e MYSQL_PORT=3306 \
  shenyu-admin:latest
```

---

## 服务启动与验证

### 1. 启动服务

```bash
cd /opt/shenyu

# 启动 Admin
./start-admin.sh

# 等待 Admin 启动完成（约 30-60 秒），然后启动 Bootstrap
./start-bootstrap.sh
```

### 2. 检查服务状态

```bash
# 查看容器状态
docker ps | grep shenyu

# 查看 Admin 日志
docker logs shenyu-admin

# 查看 Bootstrap 日志
docker logs shenyu-bootstrap

# 实时查看日志
docker logs -f shenyu-admin
docker logs -f shenyu-bootstrap
```

### 3. 健康检查

```bash
# 检查 Admin 健康状态
curl http://localhost:9095/actuator/health

# 检查 Bootstrap 健康状态
curl http://localhost:9195/actuator/health

# 访问 Admin 管理界面
# 浏览器打开: http://<服务器IP>:9095
# 默认用户名/密码: admin / 123456
```

### 4. 服务管理命令

```bash
cd /opt/shenyu

# 停止所有服务
./stop-all.sh

# 重启所有服务
./restart-all.sh

# 单独重启 Admin
docker restart shenyu-admin

# 单独重启 Bootstrap
docker restart shenyu-bootstrap
```

---

## 监控部署（可选）

项目支持使用 Prometheus + Grafana 进行监控。

### 1. 启动监控服务

```bash
cd /opt/shenyu

# 启动 Prometheus 和 Grafana
./start-monitoring.sh
```

### 2. 访问监控界面

- **Grafana**: http://<服务器IP>:3000
  - 默认用户名: `admin`
  - 默认密码: `admin123`

- **Prometheus**: http://<服务器IP>:9090

### 3. 停止监控服务

```bash
./stop-monitoring.sh
```

### 4. 配置 Grafana 数据源

1. 登录 Grafana
2. 进入 Configuration > Data Sources
3. 添加 Prometheus 数据源
4. URL: `http://prometheus:9090`
5. 保存并测试连接

---

## 故障排除

### 1. 镜像架构不匹配

**问题**: 容器启动失败，提示架构不匹配

**解决方案**:
```bash
# 检查服务器架构
uname -m

# 检查镜像架构
docker image inspect shenyu-admin:latest --format='{{.Architecture}}'

# 确保镜像架构与服务器架构匹配
# x86_64 服务器使用 x86 镜像
# aarch64 服务器使用 ARM 镜像
```

### 2. 数据库连接失败

**问题**: Admin 启动失败，无法连接 MySQL

**解决方案**:
1. 检查 MySQL 服务是否运行
2. 检查数据库配置是否正确
3. 检查网络连通性: `telnet <数据库IP> 3306`
4. 检查数据库用户权限
5. 确保数据库 `shenyu` 已创建

### 3. WebSocket 连接失败

**问题**: Bootstrap 无法连接到 Admin 的 WebSocket

**解决方案**:
1. 检查 Admin 是否正常启动
2. 检查 `application.yml` 中的 WebSocket 配置
3. 检查防火墙规则
4. 检查 `allowOrigins` 配置是否正确

### 4. 端口被占用

**问题**: 容器启动失败，端口已被占用

**解决方案**:
```bash
# 检查端口占用
netstat -tulpn | grep 9095
netstat -tulpn | grep 9195

# 停止占用端口的服务或修改配置中的端口
```

### 5. 容器无法启动

**问题**: Docker 容器启动后立即退出

**解决方案**:
```bash
# 查看容器日志
docker logs shenyu-admin
docker logs shenyu-bootstrap

# 检查配置文件语法
# 确保 YAML 格式正确，无语法错误

# 检查挂载的目录权限
ls -la /opt/shenyu/configs/
ls -la /opt/shenyu/logs/
```

### 6. 内存不足

**问题**: 容器因内存不足被杀死

**解决方案**:
1. 检查系统内存: `free -h`
2. 增加 Docker 内存限制
3. 调整 JVM 参数（在启动脚本中）

### 7. 镜像加载失败

**问题**: `docker load` 命令失败

**解决方案**:
```bash
# 检查 tar 文件完整性
ls -lh offline-images/x86/*.tar

# 手动加载镜像
cd offline-images/x86
docker load -i alpine.tar
docker load -i amazoncorretto-17-alpine.tar
docker load -i shenyu-admin-x86.tar
docker load -i shenyu-bootstrap-x86.tar

# 重新标记镜像
docker tag shenyu-admin:latest-x86 shenyu-admin:latest
docker tag shenyu-bootstrap:latest-x86 shenyu-bootstrap:latest
```

---

## 快速参考

### 完整部署流程总结

```bash
# 1. 本地构建
mvn clean package -Prelease -Drat.skip=true -Dcheckstyle.skip=true -Dmaven.javadoc.skip=true -Dmaven.test.skip=true

# 2. 构建 Docker 镜像
./build-x86.sh          # 或 ./build-arm.sh

# 3. 保存镜像
./save-images.sh x86    # 或 ./save-images.sh arm

# 4. 创建部署包
./create-deployment-package.sh x86

# 5. 传输到服务器
scp shenyu-deployment-package-*.tar.gz user@server:/tmp/

# 6. 在服务器上部署
ssh user@server
cd /tmp
tar -xzf shenyu-deployment-package-*.tar.gz
cd shenyu-deployment-package-*
./deploy-server.sh

# 7. 配置数据库
vi /opt/shenyu/configs/admin/application-mysql.yml

# 8. 启动服务
cd /opt/shenyu
./start-admin.sh
./start-bootstrap.sh

# 9. 验证
curl http://localhost:9095/actuator/health
curl http://localhost:9195/actuator/health
```

### 常用命令

```bash
# 查看容器状态
docker ps -a | grep shenyu

# 查看日志
docker logs -f shenyu-admin
docker logs -f shenyu-bootstrap

# 进入容器
docker exec -it shenyu-admin sh
docker exec -it shenyu-bootstrap sh

# 重启服务
cd /opt/shenyu && ./restart-all.sh

# 停止服务
cd /opt/shenyu && ./stop-all.sh
```

---

## 注意事项

1. **架构匹配**: 确保 Docker 镜像架构与服务器架构匹配
2. **数据库准备**: 部署前确保 MySQL 数据库已准备就绪
3. **网络配置**: 确保服务器防火墙允许 9095 和 9195 端口
4. **资源要求**: 确保服务器有足够的 CPU、内存和磁盘空间
5. **配置文件**: 部署后必须修改配置文件中的占位符为实际值
6. **日志管理**: 定期清理日志文件，避免磁盘空间不足
7. **备份配置**: 建议定期备份 `/opt/shenyu/configs/` 目录

---

**文档版本**: 1.0  
**最后更新**: 2025-11-17  
**适用版本**: Apache ShenYu 2.7.0.2-SNAPSHOT

